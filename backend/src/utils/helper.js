// src/utils/helper.js
// Placeholder utilities for the backend. Add shared helper functions here.

// Example helper: simple wrapper to format API responses (expand as needed)
exports.formatResponse = (data, message = 'ok') => ({ message, data });

// TODO: add more helpers (validation, pagination, etc.)
